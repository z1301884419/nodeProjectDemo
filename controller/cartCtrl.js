const dbConfig = require('../config/dbConfig');
module.exports = {
    cartInfo(req, resp) {
        console.log(123);
        console.log(req);
        if(req.session.user){
            
        }
        
    }
}